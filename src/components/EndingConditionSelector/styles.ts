import { createStyles } from '@material-ui/core'

export default () => {
  return createStyles({
    occurrencesNumber: {
      '& p': {
        fontSize: '0.8rem'
      }
    },
    radioGroup: {},
    radio: {},
    endDate: {}
  })
}
