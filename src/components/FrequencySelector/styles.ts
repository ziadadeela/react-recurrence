import { createStyles } from '@material-ui/core'

export default () => {
  return createStyles({
    frequency: {
      marginBottom: 10
    },
    dropdown: {
      width: 100
    },
    numberOfRepetitionsInput: {},
    weekDaysSelector: {}
  })
}
